// Configuration globale du jeu
export const config = {
  characterSpeed: 3,
  storyInterval: 1200,
  autoWalk: false,
  worldLength: 10200, // Augmenté pour accommoder 8 points d'histoire
};

// Configuration du canvas
export const canvasConfig = {
  groundY: 60, // Distance du sol depuis le bas
};
